package botBank.model;

/**
 * TransactionType represents the type of a financial transaction, which can be either a DEPOSIT or a TRANSFER.
 */

public enum TransactionType {
    DEPOSIT,
    TRANSFER
}
